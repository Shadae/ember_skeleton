<h1>Plants Directory</h1>
<div class='row'>
  <div class='col-lg-2'>
    <h4>All Plants</h4>
    <ul class='list-unstyled'>
      {{# each }}
        <li>
          {{# link-to "plant" this }}
            {{ name }}
          {{/ link-to }}
        </li>
      {{/ each }}
    </ul>
  </div>
  <div class='col-lg-10'>
    {{ outlet }}
  </div>
</div>